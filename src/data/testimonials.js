/**
 * Testimonials Data
 *
 * To add a testimonial, push an object with this schema:
 * {
 *   name: string,        — Full name of the person
 *   role: string,        — Their job title / role
 *   company: string,     — Company or affiliation
 *   message: string,     — The testimonial quote
 *   avatar: string|null, — URL to avatar image (optional, null for initials fallback)
 *   rating: number,      — Star rating 1–5 (optional, defaults to 5)
 * }
 *
 * Example:
 * {
 *   name: 'Jane Doe',
 *   role: 'CTO',
 *   company: 'TechCorp',
 *   message: 'Krishna delivered an exceptional frontend experience...',
 *   avatar: null,
 *   rating: 5,
 * }
 */

export const testimonials = [];
